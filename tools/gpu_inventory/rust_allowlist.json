{
  "_comment": "Inert third-party reference allowlist for the no-Rust-dependency verifier (design doc 0053). Rust source and Cargo metadata under these prefixes is reviewed upstream reference material that must never be compiled, linked, or executed. Build rules must not reference the tiny-skia code snapshot; the resvg-test-suite SVG/PNG corpus remains legitimate test data.",
  "inertReferencePrefixes": [
    "third_party/resvg-test-suite/",
    "third_party/tiny-skia-cpp/third_party/tiny-skia/"
  ]
}
