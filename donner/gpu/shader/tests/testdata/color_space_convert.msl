// Generated by donner::gpu::shader::MslEmitter. Do not edit.

#include <metal_stdlib>

using namespace metal;

struct ColorSpaceConvertParams {
  uint direction;
  uint pad0;
  uint pad1;
  uint pad2;
};

float srgb_channel_to_linear(texture2d<float> inputTexture, texture2d<float, access::write> outputTexture, constant ColorSpaceConvertParams& params, float c) {
  if ((c <= 0.04045f)) {
    return (c / 12.92f);
  }
  return pow(((c + 0.055f) / 1.055f), 2.4f);
}

float linear_channel_to_srgb(texture2d<float> inputTexture, texture2d<float, access::write> outputTexture, constant ColorSpaceConvertParams& params, float c) {
  if ((c <= 0.0031308f)) {
    return (c * 12.92f);
  }
  return ((1.055f * pow(c, 0.41666666f)) - 0.055f);
}

kernel void cs_main(uint3 gid [[thread_position_in_grid]], texture2d<float> inputTexture [[texture(0)]], texture2d<float, access::write> outputTexture [[texture(1)]], constant ColorSpaceConvertParams& params [[buffer(3)]]) {
  const uint2 extent = uint2(outputTexture.get_width(), outputTexture.get_height());
  if (((gid.x >= extent.x) || (gid.y >= extent.y))) {
    return;
  }
  const int2 coords = int2(gid.xy);
  const float4 source = inputTexture.read(uint2(coords), uint(0));
  float4 straight = float4(0.0f);
  if ((source.w > 0.0f)) {
    straight = float4((source.xyz / source.w), source.w);
  }
  float3 color = float3(0.0f);
  if ((params.direction == 0u)) {
    color = float3(srgb_channel_to_linear(inputTexture, outputTexture, params, straight.x), srgb_channel_to_linear(inputTexture, outputTexture, params, straight.y), srgb_channel_to_linear(inputTexture, outputTexture, params, straight.z));
  } else {
    color = float3(linear_channel_to_srgb(inputTexture, outputTexture, params, straight.x), linear_channel_to_srgb(inputTexture, outputTexture, params, straight.y), linear_channel_to_srgb(inputTexture, outputTexture, params, straight.z));
  }
  const float4 result = float4((color * straight.w), straight.w);
  outputTexture.write(saturate(result), uint2(coords));
}
