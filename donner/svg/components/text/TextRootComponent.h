#pragma once
/// @file

namespace donner::svg::components {

/**
 * Defines the start of a text element, which may have other text elements as children. Created on
 * each \ref xml_text element.
 */
struct TextRootComponent {};

}  // namespace donner::svg::components
