load("@aspect_rules_js//js:defs.bzl", "js_test")
load("@npm//:defs.bzl", "npm_link_all_packages")
load("@npm//donner/editor/wasm/tests:@playwright/test/package_json.bzl", playwright_bin = "bin")

package(default_visibility = ["//visibility:public"])

npm_link_all_packages(name = "node_modules")

playwright_bin.playwright_test(
    name = "chromium_remote_smoke",
    size = "large",
    args = [
        "test",
        "$(rootpath :smoke.spec.ts)",
        "--config=$(rootpath :playwright.bazel.config.js)",
        "--grep=production Geode wasm presents visible editor pixels",
    ],
    copy_data_to_bin = False,
    data = [
        "canvas-color-stats.ts",
        "gesture-streams.ts",
        "package.json",
        "playwright.bazel.config.js",
        "playwright.config.js",
        "remote-webserver.mjs",
        "smoke.spec.ts",
        ":node_modules/@playwright/test",
        "//donner/editor/wasm:_wasm_web_package_for_serve",
        "@playwright//:chromium",
    ],
    env = {
        "CI": "1",
        "DONNER_WASM_PACKAGE_DIR": "$(rootpath //donner/editor/wasm:_wasm_web_package_for_serve)",
        "DONNER_WASM_REQUIRE_WEBGPU": "1",
        "DONNER_WASM_TEST_SERVER": "$(rootpath :remote-webserver.mjs)",
        # The repo-wide remote Emscripten workaround disables Node's WebAssembly
        # runtime, but Playwright's HTTP client uses its bundled llhttp Wasm.
        "NODE_OPTIONS": "",
        "PLAYWRIGHT_BROWSERS_PATH": "$(rootpath @playwright//:chromium)/../",
    },
    # macOS-only: the selected smoke test asserts on presented WebGPU canvas
    # pixels, and headless Chromium on Linux cannot present a WebGPU
    # OffscreenCanvas swapchain with the SwiftShader WebGPU adapter. The GPU
    # service finds no SharedImageBackingFactory for the swapchain allocation
    # (usage WebgpuSwapChainTexture|DisplayRead|...), fails the first present,
    # and destroys the device, after which every WebGPU call rejects with "A
    # valid external Instance reference no longer exists". A browser-only
    # probe (worker + transferred OffscreenCanvas + clear + present) fails the
    # same way with no Donner code involved, on both Chromium 147 and 151 and
    # with ANGLE/SwiftShader GL, Vulkan, and Graphite flag configurations, so
    # this is an environment capability boundary, not a flag or timing gap.
    # macOS headless Chromium presents through the real GPU and passes.
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)

# The browser presentation regressions, reachable from Bazel.
#
# This lane exists because the invariants it pins - among them the zoom storm
# that must never uncover the editor background under the presented document -
# were only ever reachable through the repository's own browser lane, so a
# failure there could be answered with a rerun instead of a diagnosis. Like the
# smoke lane it asserts on presented WebGPU canvas pixels, so it carries the
# same platform boundary for the same reason documented above.
playwright_bin.playwright_test(
    name = "browser_presentation_regression_test",
    size = "large",
    args = [
        "test",
        "$(rootpath :browser-presentation-regression.spec.ts)",
        "--config=$(rootpath :playwright.bazel.config.js)",
    ],
    copy_data_to_bin = False,
    data = [
        "browser-presentation-regression.spec.ts",
        "canvas-color-stats.ts",
        "gesture-streams.ts",
        "package.json",
        "playwright.bazel.config.js",
        "playwright.config.js",
        "remote-webserver.mjs",
        "surface-frame-probe.ts",
        ":node_modules/@playwright/test",
        "//donner/editor/wasm:_wasm_web_package_for_serve",
        "@playwright//:chromium",
    ],
    env = {
        "CI": "1",
        "DONNER_WASM_PACKAGE_DIR": "$(rootpath //donner/editor/wasm:_wasm_web_package_for_serve)",
        "DONNER_WASM_REQUIRE_WEBGPU": "1",
        "DONNER_WASM_TEST_SERVER": "$(rootpath :remote-webserver.mjs)",
        # The repo-wide remote Emscripten workaround disables Node's WebAssembly
        # runtime, but Playwright's HTTP client uses its bundled llhttp Wasm.
        "NODE_OPTIONS": "",
        "PLAYWRIGHT_BROWSERS_PATH": "$(rootpath @playwright//:chromium)/../",
    },
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)

# This presentation gate shares the macOS WebGPU capability requirement of the smoke lane.
playwright_bin.playwright_test(
    name = "catalog_font_loading_test",
    size = "large",
    args = [
        "test",
        "$(rootpath :smoke.spec.ts)",
        "--config=$(rootpath :playwright.bazel.config.js)",
        "--grep=catalog.font.loading|welcome.picker.paints.before|WGPU.diagnostics.do.not.block",
    ],
    copy_data_to_bin = False,
    data = [
        "canvas-color-stats.ts",
        "gesture-streams.ts",
        "package.json",
        "playwright.bazel.config.js",
        "playwright.config.js",
        "remote-webserver.mjs",
        "smoke.spec.ts",
        ":node_modules/@playwright/test",
        "//donner/editor/wasm:_wasm_web_package_for_serve",
        "@playwright//:chromium",
    ],
    env = {
        "CI": "1",
        "DONNER_WASM_PACKAGE_DIR": "$(rootpath //donner/editor/wasm:_wasm_web_package_for_serve)",
        "DONNER_WASM_REQUIRE_WEBGPU": "1",
        "DONNER_WASM_TEST_SERVER": "$(rootpath :remote-webserver.mjs)",
        "NODE_OPTIONS": "",
        "PLAYWRIGHT_BROWSERS_PATH": "$(rootpath @playwright//:chromium)/../",
    },
    tags = ["no-local"],
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)

playwright_bin.playwright_test(
    name = "browser_responsiveness_perf_test",
    size = "large",
    timeout = "long",
    args = [
        "test",
        "$(rootpath :browser-responsiveness.perf.ts)",
        "--config=$(rootpath :playwright.responsiveness.bazel.config.js)",
    ],
    copy_data_to_bin = False,
    data = [
        "browser-responsiveness.perf.ts",
        "package.json",
        "playwright.bazel.config.js",
        "playwright.config.js",
        "playwright.responsiveness.bazel.config.js",
        "prepare-browser-archives.js",
        "remote-webserver.mjs",
        ":node_modules/@playwright/test",
        "//donner/editor/wasm:_wasm_web_package_for_serve",
        "@playwright-firefox-mac14-arm64//file",
        "@playwright_chromium_mac_arm64//file",
    ],
    env = {
        "CI": "1",
        "DONNER_WASM_PACKAGE_DIR": "$(rootpath //donner/editor/wasm:_wasm_web_package_for_serve)",
        "DONNER_WASM_TEST_SERVER": "$(rootpath :remote-webserver.mjs)",
        "NODE_OPTIONS": "",
        "DONNER_CHROMIUM_ARCHIVE": "$(rootpath @playwright_chromium_mac_arm64//file)",
        "DONNER_FIREFOX_ARCHIVE": "$(rootpath @playwright-firefox-mac14-arm64//file)",
    },
    tags = [
        "manual",
        # Firefox cannot initialize its macOS sandbox inside Bazel's sandbox.
        "no-sandbox",
        "perf",
    ],
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)

playwright_bin.playwright_test(
    name = "boot_presentation_test",
    size = "large",
    timeout = "moderate",
    args = [
        "test",
        "$(rootpath :boot-presentation.spec.ts)",
        "$(rootpath :browser-backend-selection.spec.ts)",
        "--config=$(rootpath :playwright.boot-presentation.bazel.config.js)",
    ],
    copy_data_to_bin = False,
    data = [
        "boot-presentation.spec.ts",
        "browser-backend-selection.spec.ts",
        "package.json",
        "playwright.bazel.config.js",
        "playwright.boot-presentation.bazel.config.js",
        "playwright.config.js",
        "remote-webserver.mjs",
        ":node_modules/@playwright/test",
        "//donner/editor/wasm:_wasm_web_package_for_serve",
        "@playwright//:chromium",
    ],
    env = {
        "CI": "1",
        "DONNER_WASM_PACKAGE_DIR": "$(rootpath //donner/editor/wasm:_wasm_web_package_for_serve)",
        "DONNER_WASM_TEST_SERVER": "$(rootpath :remote-webserver.mjs)",
        "NODE_OPTIONS": "",
        "PLAYWRIGHT_BROWSERS_PATH": "$(rootpath @playwright//:chromium)/../",
    },
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)

# The same Chromium gates against the package that selects the browser backend for headless work,
# so the raster worker, its offscreen targets and the snapshot capture contexts beside it render
# through the Donner GPU runtime's browser backend while the window stays on the transitional
# adapter. The boot lane also runs the check that the worker selected that backend, so a package
# that quietly stayed on the adapter cannot pass these as if it had not.
playwright_bin.playwright_test(
    name = "chromium_remote_smoke_browser_backend",
    size = "large",
    args = [
        "test",
        "$(rootpath :smoke.spec.ts)",
        "--config=$(rootpath :playwright.bazel.config.js)",
        "--grep=production Geode wasm presents visible editor pixels",
    ],
    copy_data_to_bin = False,
    data = [
        "canvas-color-stats.ts",
        "gesture-streams.ts",
        "package.json",
        "playwright.bazel.config.js",
        "playwright.config.js",
        "remote-webserver.mjs",
        "smoke.spec.ts",
        ":node_modules/@playwright/test",
        "//donner/editor/wasm:_wasm_web_package_browser_backend_for_serve",
        "@playwright//:chromium",
    ],
    env = {
        "CI": "1",
        "DONNER_WASM_PACKAGE_DIR": "$(rootpath //donner/editor/wasm:_wasm_web_package_browser_backend_for_serve)",
        "DONNER_WASM_REQUIRE_WEBGPU": "1",
        "DONNER_WASM_TEST_SERVER": "$(rootpath :remote-webserver.mjs)",
        # The repo-wide remote Emscripten workaround disables Node's WebAssembly
        # runtime, but Playwright's HTTP client uses its bundled llhttp Wasm.
        "NODE_OPTIONS": "",
        "PLAYWRIGHT_BROWSERS_PATH": "$(rootpath @playwright//:chromium)/../",
    },
    # macOS-only, for the reason given on chromium_remote_smoke.
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)

playwright_bin.playwright_test(
    name = "browser_presentation_regression_browser_backend_test",
    size = "large",
    args = [
        "test",
        "$(rootpath :browser-presentation-regression.spec.ts)",
        "--config=$(rootpath :playwright.bazel.config.js)",
    ],
    copy_data_to_bin = False,
    data = [
        "browser-presentation-regression.spec.ts",
        "canvas-color-stats.ts",
        "gesture-streams.ts",
        "package.json",
        "playwright.bazel.config.js",
        "playwright.config.js",
        "remote-webserver.mjs",
        "surface-frame-probe.ts",
        ":node_modules/@playwright/test",
        "//donner/editor/wasm:_wasm_web_package_browser_backend_for_serve",
        "@playwright//:chromium",
    ],
    env = {
        "CI": "1",
        "DONNER_WASM_PACKAGE_DIR": "$(rootpath //donner/editor/wasm:_wasm_web_package_browser_backend_for_serve)",
        "DONNER_WASM_REQUIRE_WEBGPU": "1",
        "DONNER_WASM_TEST_SERVER": "$(rootpath :remote-webserver.mjs)",
        # The repo-wide remote Emscripten workaround disables Node's WebAssembly
        # runtime, but Playwright's HTTP client uses its bundled llhttp Wasm.
        "NODE_OPTIONS": "",
        "PLAYWRIGHT_BROWSERS_PATH": "$(rootpath @playwright//:chromium)/../",
    },
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)

playwright_bin.playwright_test(
    name = "catalog_font_loading_browser_backend_test",
    size = "large",
    args = [
        "test",
        "$(rootpath :smoke.spec.ts)",
        "--config=$(rootpath :playwright.bazel.config.js)",
        "--grep=catalog.font.loading|welcome.picker.paints.before|WGPU.diagnostics.do.not.block",
    ],
    copy_data_to_bin = False,
    data = [
        "canvas-color-stats.ts",
        "gesture-streams.ts",
        "package.json",
        "playwright.bazel.config.js",
        "playwright.config.js",
        "remote-webserver.mjs",
        "smoke.spec.ts",
        ":node_modules/@playwright/test",
        "//donner/editor/wasm:_wasm_web_package_browser_backend_for_serve",
        "@playwright//:chromium",
    ],
    env = {
        "CI": "1",
        "DONNER_WASM_PACKAGE_DIR": "$(rootpath //donner/editor/wasm:_wasm_web_package_browser_backend_for_serve)",
        "DONNER_WASM_REQUIRE_WEBGPU": "1",
        "DONNER_WASM_TEST_SERVER": "$(rootpath :remote-webserver.mjs)",
        "NODE_OPTIONS": "",
        "PLAYWRIGHT_BROWSERS_PATH": "$(rootpath @playwright//:chromium)/../",
    },
    tags = ["no-local"],
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)

playwright_bin.playwright_test(
    name = "boot_presentation_browser_backend_test",
    size = "large",
    timeout = "moderate",
    args = [
        "test",
        "$(rootpath :boot-presentation.spec.ts)",
        "$(rootpath :browser-backend-selection.spec.ts)",
        "--config=$(rootpath :playwright.boot-presentation.bazel.config.js)",
    ],
    copy_data_to_bin = False,
    data = [
        "boot-presentation.spec.ts",
        "browser-backend-selection.spec.ts",
        "package.json",
        "playwright.bazel.config.js",
        "playwright.boot-presentation.bazel.config.js",
        "playwright.config.js",
        "remote-webserver.mjs",
        ":node_modules/@playwright/test",
        "//donner/editor/wasm:_wasm_web_package_browser_backend_for_serve",
        "@playwright//:chromium",
    ],
    env = {
        "CI": "1",
        "DONNER_WASM_EXPECTED_HEADLESS_BACKEND": "browser",
        "DONNER_WASM_PACKAGE_DIR": "$(rootpath //donner/editor/wasm:_wasm_web_package_browser_backend_for_serve)",
        "DONNER_WASM_TEST_SERVER": "$(rootpath :remote-webserver.mjs)",
        "NODE_OPTIONS": "",
        "PLAYWRIGHT_BROWSERS_PATH": "$(rootpath @playwright//:chromium)/../",
    },
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)

playwright_bin.playwright_test(
    name = "firefox_composited_invariants_test",
    size = "large",
    timeout = "long",
    args = [
        "test",
        "$(rootpath :composited-invariants.spec.ts)",
        "$(rootpath :composited-drag-invariants.spec.ts)",
        "--config=$(rootpath :playwright.composited-firefox.bazel.config.js)",
    ],
    copy_data_to_bin = False,
    data = [
        "canvas-color-stats.ts",
        "composited-drag-invariants.spec.ts",
        "composited-invariants.spec.ts",
        "composited-pixel-classifier.mjs",
        "composited-probe.ts",
        "composited-probe-evidence.mjs",
        "gesture-streams.ts",
        "package.json",
        "playwright.bazel.config.js",
        "playwright.composited-firefox.bazel.config.js",
        "playwright.composited-firefox.config.js",
        "playwright.config.js",
        "remote-webserver.mjs",
        ":node_modules/@playwright/test",
        "//donner/editor/wasm:_wasm_web_package_for_serve",
        "@playwright//:firefox",
    ],
    env = {
        "CI": "1",
        "DONNER_WASM_PACKAGE_DIR": "$(rootpath //donner/editor/wasm:_wasm_web_package_for_serve)",
        "DONNER_WASM_REQUIRE_WEBGPU": "1",
        "DONNER_WASM_TEST_SERVER": "$(rootpath :remote-webserver.mjs)",
        "NODE_OPTIONS": "",
        "PLAYWRIGHT_BROWSERS_PATH": "$(rootpath @playwright//:firefox)/../",
    },
    tags = [
        "manual",
        "no-local",
    ],
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)

playwright_bin.playwright_test(
    name = "chromium_composited_invariants_test",
    size = "large",
    timeout = "long",
    args = [
        "test",
        "$(rootpath :composited-invariants.spec.ts)",
        "$(rootpath :composited-drag-invariants.spec.ts)",
        "--config=$(rootpath :playwright.composited-chromium.bazel.config.js)",
    ],
    copy_data_to_bin = False,
    data = [
        "canvas-color-stats.ts",
        "composited-drag-invariants.spec.ts",
        "composited-invariants.spec.ts",
        "composited-pixel-classifier.mjs",
        "composited-probe.ts",
        "composited-probe-evidence.mjs",
        "gesture-streams.ts",
        "package.json",
        "playwright.bazel.config.js",
        "playwright.composited-chromium.bazel.config.js",
        "playwright.composited-chromium.config.js",
        "playwright.config.js",
        "remote-webserver.mjs",
        ":node_modules/@playwright/test",
        "//donner/editor/wasm:_wasm_web_package_for_serve",
        "@playwright//:chromium",
    ],
    env = {
        "CI": "1",
        "DONNER_WASM_PACKAGE_DIR": "$(rootpath //donner/editor/wasm:_wasm_web_package_for_serve)",
        "DONNER_WASM_REQUIRE_WEBGPU": "1",
        "DONNER_WASM_TEST_SERVER": "$(rootpath :remote-webserver.mjs)",
        "NODE_OPTIONS": "",
        "PLAYWRIGHT_BROWSERS_PATH": "$(rootpath @playwright//:chromium)/../",
    },
    tags = [
        "manual",
        "no-local",
    ],
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)

js_test(
    name = "browser_archive_setup_tests",
    size = "small",
    copy_data_to_bin = False,
    data = [
        "prepare-browser-archives.js",
        "@playwright-firefox-mac14-arm64//file",
        "@playwright_chromium_mac_arm64//file",
    ],
    entry_point = "browser-archive-setup.spec.mjs",
    env = {
        "DONNER_CHROMIUM_ARCHIVE": "$(rootpath @playwright_chromium_mac_arm64//file)",
        "DONNER_FIREFOX_ARCHIVE": "$(rootpath @playwright-firefox-mac14-arm64//file)",
    },
    node_options = ["--test"],
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)

js_test(
    name = "composited_pixel_classifier_tests",
    size = "small",
    copy_data_to_bin = False,
    data = ["composited-pixel-classifier.mjs"],
    entry_point = "composited-pixel-classifier.spec.mjs",
    node_options = ["--test"],
)

# Drives the canvas surface path of the GPU bridge's JavaScript library against a stub canvas and
# a stub GPU device, so the library's own code runs under the normal test gate rather than only in
# a browser. The C++ half of the bridge is covered separately by //donner/gpu/browser:browser_tests.
js_test(
    name = "browser_bridge_surface_tests",
    size = "small",
    copy_data_to_bin = False,
    data = [
        "bridge-library-harness.mjs",
        "//donner/gpu/browser:library_donner_gpu.js",
    ],
    entry_point = "browser-bridge-surface.spec.mjs",
    node_options = ["--test"],
)

# Drives the device half of the same library with several logical devices in one worker, as the
# renderer and the snapshot capture context beside it hold: each must keep its own state and
# never refuse, overwrite or tear down another's.
js_test(
    name = "browser_bridge_device_tests",
    size = "small",
    copy_data_to_bin = False,
    data = [
        "bridge-library-harness.mjs",
        "//donner/gpu/browser:library_donner_gpu.js",
    ],
    entry_point = "browser-bridge-devices.spec.mjs",
    node_options = ["--test"],
)

js_test(
    name = "playwright_bazel_config_tests",
    size = "small",
    copy_data_to_bin = False,
    data = [
        "playwright.bazel.config.js",
        "playwright.boot-presentation.bazel.config.js",
        "playwright.composited-chromium.bazel.config.js",
        "playwright.composited-chromium.config.js",
    ],
    entry_point = "playwright-bazel-config.spec.mjs",
    node_options = ["--test"],
)

# Launch both browser archives on the supported CI platforms, so a fresh fetch
# is followed by an actual browser start instead of only an unzip action.
js_test(
    name = "linux_browser_archive_tests",
    size = "medium",
    copy_data_to_bin = False,
    data = [
        ":node_modules/@playwright/test",
        "@playwright//:chromium",
        "@playwright//:chromium-headless-shell",
    ],
    entry_point = "browser-archive-check.mjs",
    env = {
        "NODE_OPTIONS": "",
        "PLAYWRIGHT_BROWSERS_PATH": "$(rootpath @playwright//:chromium)/../",
    },
    node_options = ["--test"],
    target_compatible_with = [
        "@platforms//cpu:x86_64",
        "@platforms//os:linux",
    ],
)

js_test(
    name = "mac_browser_archive_tests",
    size = "medium",
    copy_data_to_bin = False,
    data = [
        ":node_modules/@playwright/test",
        "@playwright//:chromium",
        "@playwright//:chromium-headless-shell",
    ],
    entry_point = "browser-archive-check.mjs",
    env = {
        "NODE_OPTIONS": "",
        "PLAYWRIGHT_BROWSERS_PATH": "$(rootpath @playwright//:chromium)/../",
    },
    node_options = ["--test"],
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)

js_test(
    name = "remote_webserver_tests",
    size = "small",
    copy_data_to_bin = False,
    data = ["remote-webserver.mjs"],
    entry_point = "remote-webserver.spec.mjs",
    node_options = ["--test"],
)

js_test(
    name = "browser_matrix_tests",
    size = "small",
    copy_data_to_bin = False,
    data = glob([
        "*.js",
        "*.json",
        "*.mjs",
        "*.ts",
        "*.yaml",
    ]) + [
        "BUILD.bazel",
        ":node_modules/@playwright/test",
        "//:.github/workflows/editor_wasm.yml",
        "//:MODULE.bazel",
        "//donner/editor/wasm:BUILD.bazel",
        "//tools:run-browser-ci.sh",
    ],
    entry_point = "browser-matrix.spec.mjs",
    # Loading Playwright config initializes its llhttp WebAssembly module.
    env = {"NODE_OPTIONS": ""},
    node_options = ["--test"],
)

js_test(
    name = "selector_tests",
    size = "small",
    copy_data_to_bin = False,
    data = glob([
        "*.js",
        "*.json",
        "*.mjs",
        "*.ts",
        "*.yaml",
    ]) + [
        "BUILD.bazel",
        ":node_modules/@playwright/test",
        "//:.github/workflows/editor_wasm.yml",
        "//:MODULE.bazel",
        "//donner/editor:AsyncRenderer.cc",
        "//donner/editor:AsyncRenderer.h",
        "//donner/editor:EditorShell.cc",
        "//donner/editor:RenderCoordinator.cc",
        "//donner/editor:RotateCursorSet.cc",
        "//donner/editor/gui:EditorWindow.cc",
        "//donner/editor/wasm:BUILD.bazel",
        "//donner/editor/wasm:editor.css",
        "//donner/editor/wasm:editor-bootstrap.js",
        "//donner/editor/wasm:enable-threads.js",
        "//donner/editor/wasm:index.html",
        "//donner/svg/renderer/geode:GeodeDevice.cc",
        "//donner/svg/renderer/geode:GeodeDevice.h",
        "//donner/svg/renderer/geode:GeodeWgpuAdapterDevice.cc",
        "//third_party/emdawnwebgpu:webgpu/src/library_webgpu.js",
        "//tools:run-browser-ci.sh",
    ],
    entry_point = "selector-tests.mjs",
    env = {"NODE_OPTIONS": ""},
    node_options = ["--test"],
)

js_test(
    name = "catalog_font_broker_tests",
    size = "small",
    copy_data_to_bin = False,
    data = [
        "catalog-fonts.spec.mjs",
        "//donner/editor:WholeAppWorkerBridge.cc",
        "//donner/editor/wasm:catalog-fonts.js",
    ],
    entry_point = "catalog-fonts.spec.mjs",
    # Node Response initializes the llhttp WebAssembly module.
    env = {"NODE_OPTIONS": ""},
    node_options = ["--test"],
)

# Manual native-browser reference evidence for font-property investigations.
playwright_bin.playwright_test(
    name = "font_reference_probe",
    size = "small",
    args = [
        "test",
        "$(rootpath :font-reference.spec.ts)",
        "--config=$(rootpath :playwright.font-reference.config.js)",
    ],
    copy_data_to_bin = False,
    data = [
        "font-reference.spec.ts",
        "package.json",
        "playwright.font-reference.config.js",
        ":node_modules/@playwright/test",
        "//third_party/resvg-test-suite:fonts",
        "@playwright//:chromium",
        "@playwright//:firefox",
    ],
    env = {
        "DONNER_REFERENCE_FONT": "third_party/resvg-test-suite/fonts/NotoSans-Regular.ttf",
        "NODE_OPTIONS": "",
        "PLAYWRIGHT_BROWSERS_PATH": "$(rootpath @playwright//:chromium)/../",
    },
    tags = [
        "manual",
        "no-local",
    ],
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)

# The standalone Geode renderer module is selected onto the browser backend through a
# package transition. The page must select that backend and paint SVG colors, not only boot.
playwright_bin.playwright_test(
    name = "standalone_geode_browser_renderer_test",
    size = "large",
    args = [
        "test",
        "$(rootpath :standalone-geode-browser-renderer.spec.ts)",
        "--config=$(rootpath :playwright.bazel.config.js)",
    ],
    copy_data_to_bin = False,
    data = [
        "package.json",
        "playwright.bazel.config.js",
        "playwright.config.js",
        "remote-webserver.mjs",
        "standalone-geode-browser-renderer.spec.ts",
        ":node_modules/@playwright/test",
        "//donner/editor/tests:standalone_geode_browser_png_compare",
        "//donner/editor/tests:testdata/browser/standalone_geode_browser_renderer.png",
        "//donner/svg/renderer/wasm:_geode_browser_test_package_for_playwright",
        "@playwright//:chromium",
    ],
    env = {
        "CI": "1",
        "DONNER_WASM_PACKAGE_DIR": "$(rootpath //donner/svg/renderer/wasm:_geode_browser_test_package_for_playwright)",
        "DONNER_BROWSER_GOLDEN_COMPARE": "$(rootpath //donner/editor/tests:standalone_geode_browser_png_compare)",
        "DONNER_BROWSER_GOLDEN_PNG": "$(rootpath //donner/editor/tests:testdata/browser/standalone_geode_browser_renderer.png)",
        "DONNER_WASM_TEST_SERVER": "$(rootpath :remote-webserver.mjs)",
        "NODE_OPTIONS": "",
        "PLAYWRIGHT_BROWSERS_PATH": "$(rootpath @playwright//:chromium)/../",
    },
    target_compatible_with = [
        "@platforms//cpu:aarch64",
        "@platforms//os:macos",
    ],
)
